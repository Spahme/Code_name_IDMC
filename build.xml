<?xml version="1.0" encoding="UTF-8"?>
<project name="CodeName-IDMC" default="default" basedir=".">
    <description>Builds, tests, and runs the project CodeName-IDMC.</description>

    <import file="nbproject/build-impl.xml"/>

    <target name="-post-compile">
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" includes="**/*.fxml,**/*.css,**/*.png,**/*.jpg,**/*.jpeg,**/*.json"/>
        </copy>
    </target>

    <target name="-post-compile-single">
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" includes="**/*.fxml,**/*.css,**/*.png,**/*.jpg,**/*.jpeg,**/*.json"/>
        </copy>
    </target>
</project>