package codename.idmc.infrastructure.network.multiplayer.protocol;

public enum MessageType {
    JOIN_LOBBY,
    LEAVE_LOBBY,
    LOBBY_STATE,
    CHAT_MESSAGE,
    CURSOR_MOVED,
    START_GAME,
    ERROR
}